* repo structure refactoring
 * create setup.py, README, etc. for the application
 * separate settings, requirements, urls, wsgi in separate files for production and development
 * templates and urls could be moved to the application instead of having them in the project
